import { TransactionTypeEntity } from './transaction-type-entity';

describe('TransactionTypeEntity', () => {
  it('should be defined', () => {
    expect(new TransactionTypeEntity()).toBeDefined();
  });
});
